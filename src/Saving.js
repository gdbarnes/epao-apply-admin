import React from "react";
import styled from "styled-components";

const Container = styled.div`
  position: fixed;
  bottom: 10px;
  right: 10px;
`;

const Saving = () => {
  return <Container>Saving</Container>;
};

export default Saving;
